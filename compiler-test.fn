
(

(eql '(let ((x 4)
            (y 8))
       (+ x y))
     (map-letexprs (lambda (x) (+ 3 x))
                   '(let ((x 1)
                          (y 5))
                      (+ x y))))

(eql '(+ 9 y)
     (replace-var '(+ x y) 'x 9))

;; TODO: Don't replace beyond an x-redefining let or lambda
)
