
(
;; Don't forget to use eql.
(eql (list 123 '())
     (read "123"))

(eql (list "abc" '())
     (read "\"abc\""))

(eql (list "foo" '())
     (read "\"foo\""))

;; A string containing a doublequote, an 'x'
;; and a backslash.
(eql (list "\"x\\" '())
     (read "\"\\\"x\\\\\""))

)
